import 'package:flutter/material.dart';

class FavoritesRecipes extends StatelessWidget {
  const FavoritesRecipes({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: const Text('Favorites Section'));
  }
}
